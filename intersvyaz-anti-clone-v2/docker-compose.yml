version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: anti_clone
      POSTGRES_USER: anti_clone_user
      POSTGRES_PASSWORD: anti_clone_password
    ports:
      - "5432:5432"
